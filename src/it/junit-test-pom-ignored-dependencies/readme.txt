test
    dependency
    ignored
        ignored3
    ignored2

All ignored* modules are NOT translatable, only ignored and ignored2 appear in <ignored-dependencies>, building
will only work if all ignored* are correctly ignored.
